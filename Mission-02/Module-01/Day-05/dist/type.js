"use strict";
{
    const add = (num1, num2) => {
        return num1 + num2;
    };
    const friendsArrayType = ["A", "B", "C"];
    console.log(friendsArrayType);
    const friendsArrayInterface = ["A", "B", "C"];
    // const friendsArrayInterfa
}
